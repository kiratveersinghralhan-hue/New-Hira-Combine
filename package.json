{
  "name": "new-hira-harvest-booking",
  "version": "18.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check:js": "node --check public/app.js && node --check cloudflare/worker.js",
    "check:deploy": "wrangler deploy --dry-run"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "devDependencies": {
    "wrangler": "4.112.0"
  }
}
